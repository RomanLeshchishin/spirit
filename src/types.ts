export interface photoSlide{
    src: string,
    heading: string,
    description: string,
    buttonText: string
}

export interface Photo{
    id: number,
    imageUrl: string,
    name: string,
    description: string
}
