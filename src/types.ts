export type photoSlide = {
    src: string,
    heading: string,
    description: string,
    buttonText: string
}

export type Photo = {
    id: number,
    imageUrl: string,
    name: string,
    description: string
}
